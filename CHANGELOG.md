# Changelog

All notable changes to `urlps` are documented here.

## [2.0.0](https://github.com/i3iorn/urlps/compare/urlps-v1.1.3...urlps-v2.0.0) (2026-09-13)


### ⚠ BREAKING CHANGES

* make the public API honest and reachable

### Added

* add .rgignore for search hygiene and update release notes for version 0.6.0 ([7bd9d81](https://github.com/i3iorn/urlps/commit/7bd9d81f45d1acc73062b39136976c8617169276))
* add a urlps CLI for checking URLs from the shell ([debdd26](https://github.com/i3iorn/urlps/commit/debdd266e7e0c1c553ea82c424a18fd831937e97))
* add a urlps CLI for checking URLs from the shell ([fa53b31](https://github.com/i3iorn/urlps/commit/fa53b319e31cb8d89bf812ee4943b032089a9d9b))
* add additional test coverage for url, security, parser, and validation modules ([72edb77](https://github.com/i3iorn/urlps/commit/72edb77dfe2f3c91dee816934348220cd10182b7))
* add canonical form validation ([7afabbb](https://github.com/i3iorn/urlps/commit/7afabbb717de217c5becd1b435e9464537319d82))
* add clear functionality to phishing database for reset capability ([d820b52](https://github.com/i3iorn/urlps/commit/d820b523a57155207382cc77cae6887744e2b883))
* add credential leakage detection ([6dade1e](https://github.com/i3iorn/urlps/commit/6dade1edc62ec2a5334ecd0e6547d49bdb84ce07))
* add dangerous port validation with override flag ([81df367](https://github.com/i3iorn/urlps/commit/81df367556c3e4a97c6019fa9e0d64e8b762fbab))
* add DNS rate limiting and phishing database configuration constants ([6af484f](https://github.com/i3iorn/urlps/commit/6af484fa019f6e61fc7d2cbe5a32e08bce506d5a))
* add DNS rate limiting to prevent DoS attacks ([62bed6f](https://github.com/i3iorn/urlps/commit/62bed6f817685797489100fdd4fc84cc22e4c844))
* add dns_rate_limiter support for security policies and update documentation ([1799c46](https://github.com/i3iorn/urlps/commit/1799c467b8a3a431e7000fcb37c8d9c1c2369e41))
* add enhanced Punycode/IDN validation ([d46b7fe](https://github.com/i3iorn/urlps/commit/d46b7fe5ae274bee8c4fb67f778643fa11e8b7f9))
* add fail-open configuration for DNS connection errors and update security policy handling ([715cc0d](https://github.com/i3iorn/urlps/commit/715cc0d8c3e149441c705fea6f15f43b37695f91))
* add get_query_param()/get_query_param_all() accessors ([d971bb8](https://github.com/i3iorn/urlps/commit/d971bb8f19cf59dd0fb1a2d4e0470c587dfa0017))
* add get_query_param()/get_query_param_all() accessors ([5689a66](https://github.com/i3iorn/urlps/commit/5689a6687dc5fc18ab5ba9ef38a0da26952d2693))
* add IPv6 zone identifier validation ([f073c3b](https://github.com/i3iorn/urlps/commit/f073c3be571d669c66a870c48d85138dfbc520c8))
* add JSON schema for audit events generated by URL parsing function ([c4f5c9b](https://github.com/i3iorn/urlps/commit/c4f5c9b4e7edf4f3e12c864c54b98263e8d88d2a))
* add parser confusion attack detection ([51f56de](https://github.com/i3iorn/urlps/commit/51f56de8f5c1446f2eb34968e250a986ff55f75a))
* add pytest-cov for enhanced test coverage reporting ([75c411d](https://github.com/i3iorn/urlps/commit/75c411d49ca097b5cfc80dac0e9dbfac7ada85a8))
* add Python 3.14 to supported/tested versions ([d2bdb7b](https://github.com/i3iorn/urlps/commit/d2bdb7b16e3ddb724cb5553853983306f27b4aed))
* add Python 3.14 to supported/tested versions ([e31c3ab](https://github.com/i3iorn/urlps/commit/e31c3ab1347588f75fb5ae84bc9ee4bbbc56e9b0))
* add query parameter injection detection ([72266b1](https://github.com/i3iorn/urlps/commit/72266b18d2b6acb483bf032d33a70e2d6be7db78))
* add RFC 3986 Section 5 reference resolution via urlps.join() ([4e83552](https://github.com/i3iorn/urlps/commit/4e83552c97986f7a37617706f8b6ac75cf835ecc))
* add Unicode normalization before validation ([ee8f5a4](https://github.com/i3iorn/urlps/commit/ee8f5a4c287e1f09481cfd2e9f21e708b150fc2f))
* add validation checks for DNS rate limiter configuration parameters ([adc18d6](https://github.com/i3iorn/urlps/commit/adc18d697e84ea3967657077941def2324f99cc8))
* allow overriding the phishing feed URL via URLPS_PHISHING_DATABASE_URL ([52653a8](https://github.com/i3iorn/urlps/commit/52653a81b10ddc400a89d68ba5a17a93425e4cbe))
* allow overriding the phishing feed URL via URLPS_PHISHING_DATABASE_URL ([00c8d16](https://github.com/i3iorn/urlps/commit/00c8d16c566f18dfb01247b2f686eaf0583b7636))
* bump version to 0.4.0 and update security features in documentation ([a7a5378](https://github.com/i3iorn/urlps/commit/a7a537825bec99eaf2aa1672701bb67fd41217a8))
* bump version to 0.5.1 and update changelog with new test coverage details ([5b9d1b9](https://github.com/i3iorn/urlps/commit/5b9d1b9914d1c85111a67df72d9aefced97ed453))
* enforce merging restrictions by allowing only 'dev' branch pull requests to main ([29322e5](https://github.com/i3iorn/urlps/commit/29322e585ac43384facd100f18835b834452f463))
* enforce merging restrictions by allowing only 'dev' branch pull… ([292bccf](https://github.com/i3iorn/urlps/commit/292bccfc7cebd07a760c1d601f3105f49524df62))
* enhance audit callback handling and add test for no-op behavior ([e1424fa](https://github.com/i3iorn/urlps/commit/e1424faaff7f14c69f9d1be10f4330bcde325b33))
* enhance DNS rate limiting with configurable options and improved error handling ([2b8e2c0](https://github.com/i3iorn/urlps/commit/2b8e2c06af21ae4613de79e5bccc7a148261e105))
* enhance DNS rate limiting with global state management and improved safety checks ([6d5d6ac](https://github.com/i3iorn/urlps/commit/6d5d6ac95e696f976ab4e877af8d333a6d5f18ae))
* enhance documentation for URL parsing functions with comprehensive security details ([67d1768](https://github.com/i3iorn/urlps/commit/67d1768a9008167d421f7ebdb393dfde9cf6daa4))
* enhance documentation for URL validation functions with performance details ([f2fa672](https://github.com/i3iorn/urlps/commit/f2fa6720a9316e898898ebf00290207dd66ab1bf))
* enhance IP safety checks with improved validation and logging ([fb1a965](https://github.com/i3iorn/urlps/commit/fb1a965bf7062bb4cae0dd03b77104b1d4d418c1))
* enhance security checks and documentation for URL validation functions ([5136641](https://github.com/i3iorn/urlps/commit/51366418baee96750d3198a5459f2c6294f19cde))
* enhance security policy definitions and validation mechanisms ([e92c98b](https://github.com/i3iorn/urlps/commit/e92c98b4d9bf1d1a864e6c33bae33798cb0f0d63))
* enhance URL handling and phishing database configuration for improved safety and functionality ([c1df6d2](https://github.com/i3iorn/urlps/commit/c1df6d2652987fb3a5b6f318c5b14f9feff96793))
* enhance URL parser confusion detection and improve security validations ([d008ac5](https://github.com/i3iorn/urlps/commit/d008ac59b32edf8bb1b9a55150c3c84404f7e3b9))
* expand dangerous scheme blocking ([630796a](https://github.com/i3iorn/urlps/commit/630796ab58c64d8c402f558dd3f817fec46d22e5))
* implement policy-based security validation for URL parsing and enhance audit logging ([8b4282e](https://github.com/i3iorn/urlps/commit/8b4282e55dcd90d75bd786451ae6f15b9e19f902))
* improve error handling and documentation for port and URL parsing functions ([6bdd122](https://github.com/i3iorn/urlps/commit/6bdd1224822149aa356ffccd55dcf863a4544c01))
* introduce PhishingDatabase class and refactor phishing database management ([6048e72](https://github.com/i3iorn/urlps/commit/6048e72fb1c15bbaf13b03416b5c1d810fa9d817))
* makes it possible to compare URL objects to strings directly. ([b855cea](https://github.com/i3iorn/urlps/commit/b855ceaa4516b0a6b9f0c5228fcf0d5fb78869b9))
* moved general helper functions to _helpers and URL specific helpers inside the class. ([23e7b54](https://github.com/i3iorn/urlps/commit/23e7b54cf938a382eb2a3cbca571e4af11002ddb))
* refactor audit handling in url.py with AuditManager integration ([fea31f9](https://github.com/i3iorn/urlps/commit/fea31f9ea6287f70954589daba02e27c583fdb46))
* refactor audit imports in __init__.py for improved clarity and organization ([5084396](https://github.com/i3iorn/urlps/commit/5084396669185aa8a0d7fc3f81bbe4b1c4d34151))
* refactor audit management with thread-safe design and improved callback handling ([4851cfb](https://github.com/i3iorn/urlps/commit/4851cfb0009ffc446b4fe20172f46a18c4909f03))
* refactor IP utility imports in dns_guard.py for improved organization ([87a7fa7](https://github.com/i3iorn/urlps/commit/87a7fa7e57b56f67e6ef49f65880a663ee862259))
* refactor phishing database tests for improved clarity and functionality ([f692f91](https://github.com/i3iorn/urlps/commit/f692f91f1aa4553772e50de10b30d2bd970b3db1))
* refactor security policy imports and enhance DNS rate limiter tests ([c2cd45f](https://github.com/i3iorn/urlps/commit/c2cd45fbd0bdc9f32c78a3a8faa992d3d8dcb572))
* refactored all security modules into a package and separated into smaller modules. ([51ccd7d](https://github.com/i3iorn/urlps/commit/51ccd7dfcfa89931676b9b7d1969bf2468da90ea))
* release version 0.5.0 with policy-based security controls and structured audit logging ([f0ee913](https://github.com/i3iorn/urlps/commit/f0ee913e095474f67034796086542af048b3e870))
* remove coverage options from pytest configuration ([fd3e2d4](https://github.com/i3iorn/urlps/commit/fd3e2d4a064e216070457487585375f55709beff))
* rename publish.yml to publish_to_pypi.yml and add Test PyPI workflow ([32f9a21](https://github.com/i3iorn/urlps/commit/32f9a21ee0da5aed5c66bf4347b4bd6432977912))
* rename test files for improved organization and add additional security tests ([717ad44](https://github.com/i3iorn/urlps/commit/717ad44a859a4a3d8976e8fdbee67cfd9f4a5d14))
* rename URL parsing module files for improved organization ([736b43b](https://github.com/i3iorn/urlps/commit/736b43be868ae5853faecf4cf8af52d1cfa778e7))
* simplify SecurityPolicy class documentation and improve policy resolution logic ([d287d33](https://github.com/i3iorn/urlps/commit/d287d330c9717d8fce5b3423ad62cdd5c18fb87c))
* update import path for parse_url to reflect new module structure ([ec68550](https://github.com/i3iorn/urlps/commit/ec6855031f236b22893a47b929bc854dba1363c3))
* update import paths to use 'src' prefix for consistency across test files ([e142ab9](https://github.com/i3iorn/urlps/commit/e142ab9a700393b6b2454be075776741077c4d3b))


### Fixed

* a few tiny fixes to satisfy pre-commit checks that had not been run for a while. ([7eee155](https://github.com/i3iorn/urlps/commit/7eee155300f41f1850c4dd4e2c92e340feb86b4f))
* correct docstring for path resolution in conftest.py ([566e301](https://github.com/i3iorn/urlps/commit/566e3016ce8af087def841b5c39d3aaafdf3f72c))
* correct maximum path length in test for extremely long components ([fff4d76](https://github.com/i3iorn/urlps/commit/fff4d767e868f471ceb95f62f8662ca40525798e))
* correct scheme detection and phishing-db retry storm; speed up hot path ([f2774c5](https://github.com/i3iorn/urlps/commit/f2774c5b774803352fce683d2c34670034fc8da4))
* correct stale packaging metadata URLs ([9be5d4e](https://github.com/i3iorn/urlps/commit/9be5d4e09e223e79d18e3c3396d9fff2060f41b8))
* correct test_bracketed_host_without_port_or_closing_bracket ([37873c0](https://github.com/i3iorn/urlps/commit/37873c03d5cc636e2654bc6e645bb03a936b0983))
* correct test_bracketed_host_without_port_or_closing_bracket ([faf1afd](https://github.com/i3iorn/urlps/commit/faf1afda491ba760e01d3cfcd0f8b6b6a1c8b6d4))
* ensure query length does not exceed maximum allowed value ([5c7d35f](https://github.com/i3iorn/urlps/commit/5c7d35f08b65f8d3b24c7f79b0f2313580d34563))
* property-based round-trip tests need policy=balanced under strict default ([ea683f5](https://github.com/i3iorn/urlps/commit/ea683f59722db612bfe1062352f6db49e87c92da))
* quote CI Python version matrix (YAML float coercion) ([11c7437](https://github.com/i3iorn/urlps/commit/11c7437f886f3d7e3c598a49c4b9a9442aa72730))
* quote CI Python version matrix to avoid YAML float coercion ([99bcf7c](https://github.com/i3iorn/urlps/commit/99bcf7cf6c95a129e48a5e0edb420c33bd8f8681))
* regenerate stale Unicode Script table for regex 2026.7.19 ([6b8ccc7](https://github.com/i3iorn/urlps/commit/6b8ccc7799a05028bd376677e32ccbfb3e1a0972))
* regenerate Unicode table with Python 3.12 to match CI ([42a5f6a](https://github.com/i3iorn/urlps/commit/42a5f6a89f3c43447a13ba76c20fa206a6675b54))
* removed development comments ([8c5d983](https://github.com/i3iorn/urlps/commit/8c5d98372c397a628ecb4e40ed72657dbe4e3a7b))
* repair broken CI/CD pipeline ([0201346](https://github.com/i3iorn/urlps/commit/02013460105c9e93acfbd6016392b855decff09e))
* repair broken CI/CD pipeline and stale packaging metadata ([0de82dd](https://github.com/i3iorn/urlps/commit/0de82dd5e1f3b91928f644cc98a1fa03ac4300fa))
* restore URL-to-string comparison lost in the 1.0.1 refactor ([856391b](https://github.com/i3iorn/urlps/commit/856391bef1d115504e168f269a538c26e2c86f19))
* satisfy ruff import order and mypy type narrowing ([0584dae](https://github.com/i3iorn/urlps/commit/0584dae131421f9d5da114294864025949a4cd8d))
* scheme-parsing bug, phishing-db retry storm, and hot-path perf ([f4e538e](https://github.com/i3iorn/urlps/commit/f4e538e8a356ffef211f620dc1f58f4f4c21625a))
* **security:** close obfuscated-IP SSRF bypass and make checks fail closed ([cef3ab1](https://github.com/i3iorn/urlps/commit/cef3ab12a28dc8333477646062b64a4a670f50bf))
* strip trailing dot before IP-literal SSRF checks ([c3b65f3](https://github.com/i3iorn/urlps/commit/c3b65f39ed76ccfdf96a5ee5bb42181bbc047dc0))
* strip trailing dot before IP-literal SSRF checks ([18c05b9](https://github.com/i3iorn/urlps/commit/18c05b94624719c9d1f554619466a0d25fd1f9a8))
* update python version to 3.10 in mypy configuration and add asyncio default fixture loop scope ([5e96544](https://github.com/i3iorn/urlps/commit/5e965446425c8df19d11b05f6408154077ff45e7))
* update test cases for maximum path length and query parameters ([e244ef9](https://github.com/i3iorn/urlps/commit/e244ef93046616207bf3deb311e81d5e086902de))
* validate host format in build()/compose_url() ([6bc538e](https://github.com/i3iorn/urlps/commit/6bc538eeb5065d16558e3eef99fe3a91cbee7b24))
* validate host format in build()/compose_url() ([4bef394](https://github.com/i3iorn/urlps/commit/4bef3943d062ac9fb83e1d31576f0b629400f7b3))
* wire up or remove the 8 never-raised exception subclasses ([a136c91](https://github.com/i3iorn/urlps/commit/a136c91a96e171f4ee6acfc613f5882b1ef1d235))
* with_scheme(None) now clears the scheme like its siblings ([6eb9147](https://github.com/i3iorn/urlps/commit/6eb91476f7c7bf65f8cf24e0dcb728652a022888))


### Changed

* bump regex from 2024.11.6 to 2026.7.19 ([d43564c](https://github.com/i3iorn/urlps/commit/d43564cbcf4e03643776d7ed01be13b2c289d36f))
* clean up code by removing commented sections and improving readability ([4842ea8](https://github.com/i3iorn/urlps/commit/4842ea8d5f53ac7619e92df4a9c04b076f028ed8))
* decompose url.py and __init__.py into focused modules ([81927d7](https://github.com/i3iorn/urlps/commit/81927d744dbefc37f6645e55e22f28885f6dc4b7))
* decompose url.py and __init__.py into focused private modules ([2cab8ed](https://github.com/i3iorn/urlps/commit/2cab8ed44adf93d0f5a9a87f5c8753bd87cae968))
* make the public API honest and reachable ([cbe4f83](https://github.com/i3iorn/urlps/commit/cbe4f8387d42bf6c05136457011e51fb7b229edc))
* rename package from urlp to urlps and update imports ([eeb3e39](https://github.com/i3iorn/urlps/commit/eeb3e39f3ced701e3ec20d24c8db6844221941b0))
* update import paths for local runs to align with src-layout structure ([8ad1fb5](https://github.com/i3iorn/urlps/commit/8ad1fb566728d514c52c64e03085e05948bef53a))
* update import paths to remove 'src' prefix for consistency ([a85d0dc](https://github.com/i3iorn/urlps/commit/a85d0dc2b4285534f3cb91a88957d212e7cdf2cd))

## 1.1.3 - 2026-09-13

**Added**

- `URL.get_query_param()`/`get_query_param_all()` accessors for reading a
  single query parameter's first value or all its values, without having
  to unpack `query_params()` for the common case.
- `urlps` CLI entry point for checking a URL's parse/validation result from
  the shell.
- Python 3.14 added to the supported/tested version matrix.
- CI now generates a CycloneDX SBOM (`urlps-sbom.json`) for every build,
  uploaded as a build artifact alongside the sdist/wheel.

**Fixed**

- Pinned `dns_rate_limiter` identity test to guard the interaction between
  `_resolve_named_policy`'s cache and `_apply_overrides`'s identity check,
  preventing a future policy field from silently reintroducing limiter
  cross-contamination between differently-configured callers.

**Changed**

- Bumped the pinned `regex` dev dependency (used only to generate the
  Unicode Script tables checked into the package; not a runtime
  dependency) to 2026.7.19.
- Removed the `only-dev-to-main.yml` workflow enforcing "only `dev` can
  merge to `master`" — that branch protection rule has been dropped in
  favor of GitHub's own branch protection settings.

## 1.1.0 - 2026-08-30

**Fixed**

- SSRF bypass: `is_ssrf_risk()` normalized the host for its hostname-blocklist
  check but not for any of its IP-literal checks, so a trailing dot on a
  numeric host (`10.0.0.5.`, `192.168.1.1.`) slipped past private-IP
  detection — the same class of bypass 1.0.0 fixed for hostnames,
  reintroduced for numeric IP forms. Not reachable through `parse_url()`
  itself (the parser already rejects a trailing dot on an IP literal as
  malformed), but reachable by anything calling `is_ssrf_risk()` directly.
- `build()`/`compose_url()` never validated the `host` component, only
  normalized it, so a malformed host could produce a string that
  `parse_url()` then refused to parse on round-trip. Host format is now
  validated the same way the parser validates it (hostname grammar, or
  IPv4/IPv6 literal), raising `HostValidationError` for a malformed host.
  Purely structural — `build_secure()` remains the way to validate an
  untrusted host.
- Regression: `URL` objects stopped comparing equal (or orderable) against
  plain strings. This worked once, then was silently dropped when
  `__eq__`/`__lt__`/`__le__`/`__gt__`/`__ge__` moved into `_comparison.py`
  during the module decomposition — every comparison there checked only
  `isinstance(other, URL)`, so `url == "https://example.com/"` returned
  `False` instead of comparing string content. Restored; comparisons
  against a string now use `as_string()` directly (no canonicalization).
- A test asserting `get_canonical_url("http://[::1]extra/path")` returns a
  non-`None` result was wrong: stdlib's `urlparse()` raises
  `ValueError: Invalid IPv6 URL` for that input before reaching the code
  path the test meant to exercise, so `get_canonical_url` correctly failed
  closed and returned `None` already. The test's premise didn't hold under
  this Python version's `urlparse()`; corrected the assertion.

**Added**

- `URLPS_PHISHING_DATABASE_URL` environment variable overrides the feed
  `check_phishing=True` downloads hostnames from (previously a single
  hardcoded third-party URL with no override), so callers can self-host the
  list or point at a mirror they trust. Read once at import time, like the
  existing cache-size/length-limit environment variables.
- CI now installs the package without the `idna` extra and runs a dedicated
  suite against the stdlib-only IDNA 2003 fallback in
  `_security/_unicode/uts46.py`, which previously had zero test coverage
  despite being a security-relevant differential (the same class of bug
  fixed in 1.0.0: `straße.de` resolves differently under IDNA 2003 vs.
  UTS-46).
- README section on using `check_dns=True`/`check_phishing=True` (both
  blocking network I/O) from `asyncio` code without stalling the event loop.
- CI now also runs on pushes and pull requests targeting `dev`, not just
  `master` — previously every feature-branch PR into `dev` got no CI signal
  at all.

**Deprecated**

- `parse_url_unsafe()` now emits `DeprecationWarning`; use `parse_url_local()`
  instead. It has been documented as the preferred name since 1.0.0's
  `parse_url_unsafe()` → `parse_url_local()` rename, but nothing actually
  warned until now. Removal planned for 2.0.
- `SecurityPolicy(enforce_suspicious_punycode=True)` now emits
  `DeprecationWarning`. The field has gated nothing since 1.0.0 (superseded
  by `enforce_mixed_scripts`/`enforce_confusable_host`'s real UTS-39
  analysis); passing `True` now says so instead of silently no-opping.
  Removal planned for 2.0.
- `has_suspicious_punycode()` now emits `DeprecationWarning`. Its heuristics
  (hardcoded suspicious-TLD/brand-name lists, ad hoc substring checks)
  predate and are weaker than the real homograph detection `parse_url()`
  actually enforces; use `urlps._security.host_analysis.analyze_host()`
  instead. Removal planned for 2.0.

See [changelogs/1.1.0.md](changelogs/1.1.0.md) for the full account.

## 1.0.1 - 2026-08-20

Internal cleanup. No public API or behavior changes.

- Decomposed `url.py` and `__init__.py` into focused private modules
  (`_comparison.py`, `_serialization.py`, `_entrypoints.py`, and others) —
  same public surface, smaller and more navigable files.
- Raised test coverage to 96%+ with targeted edge-case and hot-path tests,
  and removed a small amount of dead code (duplicate `idna`-detection
  variables in `_validation.py` that were never read).

See [changelogs/1.0.1.md](changelogs/1.0.1.md) for the full account.

## 1.0.0 - 2026-08-19

The theme is one correction: **urlps was rejecting where it should have been
normalizing.** Canonicalization was a validation gate rather than a
transformation, which simultaneously made the library hostile to ordinary
input (`HTTP://EXAMPLE.COM/` and `https://example.com:443/` were rejected)
*and* left a real bypass — with that gate off, `url.host` came back
un-normalized, so `EVIL.COM` and `evil.com.` slipped past a caller's
blocklist. Normalizing fixes both.

**Security fixes**

- Host normalization bypass: `url.host` is now always canonical under every
  policy, so allowlist comparisons are reliable.
- `URL(...)` defaulted to a near-no-op policy while `parse_url()` defaulted to
  `strict`, making the class a quiet way to skip every check. Both now use
  `strict`.
- `SecurityPolicy.internal()` silently disabled SSRF; it no longer does.
- Punycode homographs received **no** script analysis (Punycode is ASCII, and
  the check was gated on `not isascii()`), so `xn--pypal-4ve.com` was accepted
  under `balanced`. The host is now decoded before analysis.
- IDNA parser differential: `straße.de` resolved to `strasse.de` rather than
  the `xn--strae-oqa.de` browsers use — a different domain.
- `URL` was documented as immutable but wasn't; it now is.

**Breaking**

- `enforce_query_injection` and `require_canonical` removed;
  `block_dangerous_ports` and `reject_credentials` are now opt-in. Each with a
  security justification and a non-bypass regression test.
- `parse_url_unsafe()` → `parse_url_local()` (alias retained), now using the
  new `local` policy, which narrows SSRF rather than disabling it.
- `URL.host` casing/trailing dot, `URL()` default policy, `internal()` SSRF,
  and `URL` immutability all change observable behaviour.

See [changelogs/1.0.0.md](changelogs/1.0.0.md) for the full account and
[MIGRATION.md](MIGRATION.md) for the upgrade path.

## 0.8.0 - 2026-08-18

**Breaking:** `parse_url()`'s default security policy is now `"strict"`
(was `"balanced"`). See [changelogs/0.8.0.md](changelogs/0.8.0.md) for the
full list of what's now blocked by default and how to opt back into the old
behavior with `policy="balanced"`.

**Added**

- `has_suspicious_punycode()` (Punycode/IDN homograph detection) is now
  actually wired into validation via a new
  `SecurityPolicy.enforce_suspicious_punycode` field (strict-only — see
  changelog for why).

**Fixed**

- `is_open_redirect_risk()` now also checks the percent-decoded path, so
  `%5cevil.com` is caught the same way a raw backslash already was.

## 0.7.1 - 2026-08-18

Robustness/maintainability follow-ups to 0.7.0. See
[changelogs/0.7.1.md](changelogs/0.7.1.md) for detail. No public API or
behavior changes.

**Added**

- A standing test that guards against the fail-open security-predicate
  pattern reappearing in `_security/`.
- Property-based testing extended to `_resolve.py` path arithmetic and
  `_parse_inet_aton_ipv4`.
- A sustained-load soak test for `DNSRateLimiter` (finally uses the `slow`
  marker).
- `Makefile`, `docs/architecture.md`, and a permanent CI check that every
  README code example still runs.

**Fixed**

- `with_scheme(None)` regression (rejected `None` unlike every sibling
  `with_*` method).
- Wired up or removed 8 exception subclasses that could never actually be
  raised.
- Cleared and permanently gated `ruff` `UP`/format debt.

## 0.7.0 - 2026-08-10

Production-readiness release. See [changelogs/0.7.0.md](changelogs/0.7.0.md) for
detail and [MIGRATION.md](MIGRATION.md) for upgrade steps.

**Security**

- Closed an SSRF bypass: obfuscated IPv4 forms (`0x7f000001`, `017700000001`,
  `0xc0a80101`) address loopback and private ranges but were accepted with SSRF
  protection enabled.
- Security checks that cannot reach a verdict now fail closed instead of
  reporting "safe".
- `check_phishing=True` no longer silently provides no protection when the
  database is unavailable; it now reports the degradation.
- DNS resolution is now bounded by the documented timeout, which previously
  applied only to the subsequent connection.
- Shared mutable state (DNS rate limiter, lazy globals) is now synchronised.

**Fixed**

- Parsing no longer rewrites query strings. The previous re-encoding was lossy
  and could turn one parameter into two — a parameter-smuggling vector.
- `with_query()`, `with_query_param()` and `without_query_param()` were silent
  no-ops and now work.
- `parse_relative_reference()` no longer rejects references that merely contain
  `://` in a query value.
- `copy()`/`with_*` now validate components as strictly as `parse_url()`.

**Added**

- `join()` for RFC 3986 Section 5 reference resolution — the security-preserving
  equivalent of `urllib.parse.urljoin`.
- Per-call audit configuration via `audit=AuditConfig(...)`. The previously
  documented `set_audit_callback()` never existed.
- `py.typed`, so the `Typing :: Typed` classifier is finally true.
- Public exports for `DNSRateLimiter`, `SecurityFinding`, `ErrorCode` and more.

**Breaking**

- `str(url)` returns the query as supplied rather than re-encoded.
- `strict=` removed from `parse_url_unsafe()` and `URL()`; use `policy=`.

## 0.6.1 - 2026-08-09

- Fixed a scheme-parsing bug where relative URLs containing `://` later in the string (e.g. `?redirect=http://host`, a very common query-parameter shape) were incorrectly rejected as invalid absolute URLs instead of being parsed as relative references.
- Fixed the same substring-matching flaw in the security scanner (`extract_host_and_path`, `has_parser_confusion`, `is_non_canonical_url`, `get_canonical_url`) so a query value can no longer be mistaken for the URL's actual host.
- Fixed the phishing-domain database retrying a network download on every single `check_phishing=True` call whenever the previous refresh had failed; added a 5-minute cooldown so an unreachable feed no longer adds blocking network latency to every parse.
- Added `dns_rate_limiter` dependency-injection support to `parse_url()`, `parse_url_unsafe()`, and `build_secure()` for isolating DNS lookup rate limits per application/tenant.
- Improved parsing performance by roughly 20% through caching of repeated security checks and micro-optimizations on hot paths, with no change in behavior.
- Fixed the CI pipeline, which had drifted out of sync with the src-layout migration and a repository rename: it was not running on pull requests or the default branch, and its `mypy`/`bandit`/`pip-audit` steps were silently scanning nothing.
- Corrected stale packaging metadata (`project.urls` pointed at the wrong GitHub owner and a nonexistent branch).

## 0.6.0 - 2026-05-25

- Standardized public imports on `urlps` across docs, tests, performance tooling, and package metadata.
- Added configurable DNS connect error behavior via `dns_fail_open_on_connect_error`.
- Updated policy defaults for DNS connect checks: strict fail-closed, balanced fail-open.
- Updated `parse_url_unsafe(policy=...)` to honor explicit policy values exactly.
- Refined audit, phishing database, DNS rate limiting, IP safety, and policy-resolution internals.
- Added search hygiene support via `.rgignore`.
- Added release notes in `changelogs/0.6.0.md`, including the full commit history since `v0.5.1`.

## 0.5.1 - 2026-06-22

- Added focused coverage suites (`tests/test_coverage_gaps.py`, `tests/test_coverage_gaps2.py`) for parser, security, validation, URL helpers, and facade flows.
- Added regression tests for defensive error-handling paths (IDNA failures, query parsing errors, DNS failures/rate limiting, parser-confusion and canonicalization branches).
- Improved confidence in secure defaults and fallback behavior without introducing breaking API changes.

## 0.5.0 - 2026-05-22

- Added policy-based security controls via `SecurityPolicy` (`strict`, `balanced`, `internal`).
- Added structured security findings, typed error codes, and richer URL validation interfaces.
- Added structured audit event callbacks with correlation-id support and safer URL redaction.
- Added secure composition helpers (`build_secure`, secure builder flow).
- Strengthened mutation-time validation for `URL.copy()` and `with_*` methods.

## 0.4.0

- Introduced major security hardening across parsing and validation.
- Added DNS rebinding checks, rate-limiting controls, and phishing-database integration.
- Expanded parser-confusion, canonicalization, and injection-related protections.
- Improved cache management and performance observability.

## 0.3

- Stabilized core parser/builder behavior and immutable `URL` workflows.
- Improved RFC 3986 alignment and expanded test coverage.

## 0.2

- Added foundational security checks and stricter validation paths.
- Improved host/IP handling and URL normalization behavior.

## 0.1

- Initial public releases of URL parsing/building APIs.
- Core immutable URL representation and component helpers.
